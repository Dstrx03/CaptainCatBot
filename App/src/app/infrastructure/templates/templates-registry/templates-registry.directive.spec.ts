import { TemplatesRegistryDirective } from './templates-registry.directive';

describe('TemplatesRegistryDirective', () => {
  it('should create an instance', () => {
    const directive = new TemplatesRegistryDirective();
    expect(directive).toBeTruthy();
  });
});
